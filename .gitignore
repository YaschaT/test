# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables and Google Cloud credentials — never commit these.
.env
.env.*
!.env.example
server/credentials/*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Design source for the dashboard redesign (46MB of reference PNGs). The build only needs the
# converted, web-sized copies in public/assets/dashboard/redesign/.
Dashboard-redesign/
